{
    "private": true,
    "type": "module",
    "engines": {
        "node": "20.x",
        "npm": "9.x"
    },
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "postinstall": "npm install && npm run build && php artisan config:cache && php artisan route:cache && php artisan migrate --force"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.20",
        "axios": "^1.7.4",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.0",
        "postcss": "^8.4.47",
        "tailwindcss": "^3.4.13",
        "vite": "^6.0"
    }
}
